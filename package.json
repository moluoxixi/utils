{
  "name": "utils",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r --filter ./packages/* build",
    "test": "pnpm run typecheck && pnpm -r --filter ./packages/* test",
    "typecheck": "pnpm -r --filter ./packages/* typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^8.1.1",
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/node": "^25.5.2",
    "eslint": "^10.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.3"
  }
}